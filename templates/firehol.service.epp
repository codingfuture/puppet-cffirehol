<% |
    String $before
|-%>
[Unit]
Description=CF: FireHOL stateful packet filtering firewall for humans
Before=network-pre.target network.target <%= $before %>
Wants=network-pre.target network.target <%= $before %>

[Service]
Type=oneshot
ExecStart=/usr/sbin/firehol start
ExecStop=/usr/sbin/firehol stop
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
